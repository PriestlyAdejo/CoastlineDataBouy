# Backend API (FastAPI)

The backend is the canonical data layer for the dashboard.

It ingests uploads from the buoy (edge), stores structured data in Postgres, stores artifacts in object storage (S3-compatible), and serves API endpoints to the dashboard.

