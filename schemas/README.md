# Schemas

Canonical, versioned data contracts shared across:

- Pi edge services (Python)
- backend ingest + API (Python)
- dashboard (TypeScript)

We keep JSON Schema as the source of truth in `schemas/jsonschema/v1`.

